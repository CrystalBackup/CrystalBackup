apiVersion: v1
kind: Service
metadata:
  labels:
    app: web
  name: web
spec:
  ipFamilyPolicy: SingleStack
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: web
  type: ClusterIP
