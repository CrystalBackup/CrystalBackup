apiVersion: crystalbackup.io/v1alpha1
kind: ClusterErasure
metadata:
  labels:
    app.kubernetes.io/name: bs-k8s-backup
    app.kubernetes.io/managed-by: kustomize
  name: clustererasure-sample
spec:
  # TODO(user): Add fields here
