# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/crystalbackup.io_clusterbackuplocations.yaml
- bases/crystalbackup.io_clusterbackupschedules.yaml
- bases/crystalbackup.io_clusterbackups.yaml
- bases/crystalbackup.io_clusterrestores.yaml
- bases/crystalbackup.io_clustererasures.yaml
- bases/crystalbackup.io_clusterbackupexternalsyncs.yaml
- bases/crystalbackup.io_backuplocations.yaml
- bases/crystalbackup.io_backupschedules.yaml
- bases/crystalbackup.io_backups.yaml
- bases/crystalbackup.io_restores.yaml
- bases/crystalbackup.io_backupexternalsyncs.yaml
- bases/crystalbackup.io_backuprepositories.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [WEBHOOK] To enable webhook, uncomment the following section
# the following config is for teaching kustomize how to do kustomization for CRDs.
#configurations:
#- kustomizeconfig.yaml
