---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: clusterbackupexternalsyncs.crystalbackup.io
spec:
  group: crystalbackup.io
  names:
    kind: ClusterBackupExternalSync
    listKind: ClusterBackupExternalSyncList
    plural: clusterbackupexternalsyncs
    shortNames:
    - cbes
    singular: clusterbackupexternalsync
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.lagSnapshots
      name: Lag
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterBackupExternalSync replicates the shared DR repo to a
          secondary location (R28).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterBackupExternalSync
            properties:
              destinationLocationRef:
                description: |-
                  destinationLocationRef is another ClusterBackupLocation with its own key
                  (must differ from source — admission rule 9).
                properties:
                  name:
                    description: name of the referent.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              mode:
                default: Mirror
                description: mode tracks the source (Mirror) or only adds (AppendOnly,
                  forced on Immutable destinations).
                enum:
                - Mirror
                - AppendOnly
                type: string
              paused:
                description: paused suspends new syncs.
                type: boolean
              schedule:
                description: schedule is a cron expression; empty ⇒ on-demand only.
                type: string
              selection:
                description: selection narrows the copy by namespace tag; omitted
                  ⇒ whole repository.
                properties:
                  namespaces:
                    description: namespaces narrows the copy by namespace tag.
                    properties:
                      exclude:
                        description: exclude is a list of glob patterns removed after
                          the positive match.
                        items:
                          type: string
                        type: array
                      matchExpressions:
                        description: matchExpressions selects namespaces by label
                          expressions.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: matchLabels selects namespaces by label.
                        type: object
                      matchNames:
                        description: matchNames is a list of glob patterns on namespace
                          names.
                        items:
                          type: string
                        type: array
                      regexp:
                        description: regexp matches namespace names (power tool; see
                          adr/0009).
                        type: string
                    type: object
                type: object
              sourceLocationRef:
                description: 'sourceLocationRef is a ClusterBackupLocation (default:
                  the default one).'
                properties:
                  name:
                    description: name of the referent.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              timezone:
                description: timezone for the cron expression (IANA name).
                type: string
            required:
            - destinationLocationRef
            - sourceLocationRef
            type: object
          status:
            description: status defines the observed state of ClusterBackupExternalSync
            properties:
              bytesCopied:
                description: bytesCopied (data streamed this run, blob-incremental).
                format: int64
                type: integer
              conditions:
                description: conditions represent the current state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lagSnapshots:
                description: lagSnapshots is the count of source snapshots not yet
                  at the destination.
                format: int32
                type: integer
              lastSuccessTime:
                description: lastSuccessTime of a completed sync.
                format: date-time
                type: string
              phase:
                description: phase of the sync.
                enum:
                - Pending
                - Running
                - Completed
                - PartiallyFailed
                - Failed
                type: string
              snapshotsCopied:
                description: snapshotsCopied in the last run.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
