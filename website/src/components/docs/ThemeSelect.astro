---
// Starlight's theme switcher, deliberately removed.
//
// The landing page (src/pages/index.astro) is dark-only, and the docs share its
// palette. Shipping a toggle would promise a light theme that nothing else on the
// site has, and a half-maintained second theme reads as a bug rather than a choice.
// src/styles/docs.css pins `:root` to the dark scale accordingly.
---
