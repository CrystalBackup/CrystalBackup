# Single-pod SeaweedFS exposing the S3 API — the object-storage backend for the e2e suite
# (spec/08-testing-and-dod.md §4, spec/90-roadmap.md M0 "SeaweedFS fast loop").
#
# `weed server -s3` runs master + volume + filer + the S3 gateway in one process, which is
# ideal for a disposable test backend. Storage is an emptyDir (ephemeral, recreated per
# cluster). A bootstrap Job pre-creates the buckets used by the two-plane fixtures so the
# data-path suites (M1+) can write immediately. The M0 CRD round-trip does NOT touch S3 —
# it only references these Secret names in specs — so the suite stays green even before any
# object is written.
---
apiVersion: v1
kind: Namespace
metadata:
  name: crystalbackup-e2e
  labels:
    app.kubernetes.io/part-of: crystal-backup-e2e
---
# SeaweedFS S3 identities/credentials (-s3.config).
#
# TWO identities, and the second one is not cosmetic. External sync (R28,
# spec/adr/0013-external-backup-sync.md) exists because restic holds two repository KEYS but only
# one set of backend CREDENTIALS — which is why both repositories are addressed through rclone
# remotes, each carrying its own. If both ends of a sync authenticated with the same access key,
# the e2e suite would exercise the rclone plumbing while proving nothing about the limitation it
# was built to escape: a bug that collapsed the two remotes onto one credential set would pass.
#
# `offsite-2` is therefore a DISTINCT access key, scoped to the destination bucket alone. It is
# also the strongest assertion available anywhere in this project's gates — the crucible cannot
# make it (Hetzner Object Storage credentials are project-wide, so a second bucket there shares
# one account), so kind is where cross-credential sync is actually proven.
apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-config
  namespace: crystalbackup-e2e
stringData:
  s3.json: |
    {
      "identities": [
        {
          "name": "crystal-e2e",
          "credentials": [
            { "accessKey": "crystalbackup", "secretKey": "crystalbackup-secret" }
          ],
          "actions": ["Admin", "Read", "Write", "List", "Tagging"]
        },
        {
          "name": "crystal-e2e-offsite",
          "credentials": [
            { "accessKey": "offsite-2", "secretKey": "offsite-2-secret" }
          ],
          "actions": [
            "Read:tenant-a-offsite-2",
            "Write:tenant-a-offsite-2",
            "List:tenant-a-offsite-2"
          ]
        }
      ]
    }
---
# Convenience Secret in the AWS-CLI/restic env-var shape, mirroring the identity above.
# Namespace-plane and cluster-plane BackupLocation CRs can point their credentialsSecretRef
# here in the data-path suites.
apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-credentials
  namespace: crystalbackup-e2e
stringData:
  AWS_ACCESS_KEY_ID: crystalbackup
  AWS_SECRET_ACCESS_KEY: crystalbackup-secret
---
# The SECOND credential set, for the external-sync destination. A BackupLocation pointing here
# authenticates as a different S3 principal from the source location — which is what makes the
# sync a genuine cross-account copy rather than two prefixes under one key.
#
# Scoped to tenant-a-offsite-2 by the identity above: if the sync ever addressed the SOURCE bucket
# with this key it would fail with AccessDenied rather than silently succeeding, so the scoping is
# an assertion in itself.
apiVersion: v1
kind: Secret
metadata:
  name: seaweedfs-s3-credentials-offsite
  namespace: crystalbackup-e2e
stringData:
  AWS_ACCESS_KEY_ID: offsite-2
  AWS_SECRET_ACCESS_KEY: offsite-2-secret
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: seaweedfs
  namespace: crystalbackup-e2e
  labels:
    app: seaweedfs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: seaweedfs
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: seaweedfs
    spec:
      containers:
        - name: seaweedfs
          # Pinned SeaweedFS release; override at will. Multi-arch (amd64/arm64) image.
          image: chrislusf/seaweedfs:3.80
          args:
            - server
            - -dir=/data
            - -ip.bind=0.0.0.0
            - -master.volumeSizeLimitMB=256
            - -volume.max=100
            - -filer
            - -s3
            - -s3.port=8333
            - -s3.config=/etc/seaweedfs/s3.json
          ports:
            - name: s3
              containerPort: 8333
            - name: master
              containerPort: 9333
            - name: filer
              containerPort: 8888
          readinessProbe:
            tcpSocket:
              port: s3
            initialDelaySeconds: 3
            periodSeconds: 3
          livenessProbe:
            tcpSocket:
              port: master
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: "1"
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: data
              mountPath: /data
            - name: s3-config
              mountPath: /etc/seaweedfs
              readOnly: true
      volumes:
        - name: data
          emptyDir: {}
        - name: s3-config
          secret:
            secretName: seaweedfs-s3-config
---
apiVersion: v1
kind: Service
metadata:
  name: seaweedfs
  namespace: crystalbackup-e2e
  labels:
    app: seaweedfs
spec:
  selector:
    app: seaweedfs
  ports:
    - name: s3
      port: 8333
      targetPort: s3
    - name: master
      port: 9333
      targetPort: master
    - name: filer
      port: 8888
      targetPort: filer
---
# Pre-create the buckets the two-plane fixtures use. Idempotent (`|| true`) and waits for
# the S3 gateway to answer before issuing CreateBucket.
apiVersion: batch/v1
kind: Job
metadata:
  name: seaweedfs-bucket-init
  namespace: crystalbackup-e2e
  labels:
    app: seaweedfs
spec:
  backoffLimit: 6
  template:
    metadata:
      labels:
        app: seaweedfs-bucket-init
    spec:
      restartPolicy: OnFailure
      containers:
        - name: awscli
          image: amazon/aws-cli:2.15.30
          env:
            - name: AWS_ACCESS_KEY_ID
              value: crystalbackup
            - name: AWS_SECRET_ACCESS_KEY
              value: crystalbackup-secret
            - name: AWS_DEFAULT_REGION
              value: us-east-1
            - name: AWS_EC2_METADATA_DISABLED
              value: "true"
            - name: ENDPOINT
              value: http://seaweedfs.crystalbackup-e2e.svc.cluster.local:8333
          command:
            - /bin/sh
            - -c
            - |
              set -eu
              echo "waiting for the SeaweedFS S3 endpoint at ${ENDPOINT} ..."
              for i in $(seq 1 60); do
                if aws --endpoint-url "${ENDPOINT}" s3 ls >/dev/null 2>&1; then
                  echo "S3 endpoint is up"; break
                fi
                sleep 2
              done
              for b in e2e-dr tenant-a-backups tenant-b-backups tenant-a-offsite-2; do
                echo "creating bucket ${b}"
                aws --endpoint-url "${ENDPOINT}" s3 mb "s3://${b}" || true
              done
              echo "buckets present:"
              aws --endpoint-url "${ENDPOINT}" s3 ls
