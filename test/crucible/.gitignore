# Generated by the crucible — everything here is machine- or run-specific.
artifacts/
ansible/inventory/
terraform/.terraform/
# All state files (incl. timestamped *.tfstate.<n>.backup from `state rm`) —
# they contain the RKE2 token. Never commit.
terraform/*.tfstate
terraform/*.tfstate.*
terraform/tfplan
*.retry
